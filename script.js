
{
    const nameGoods = "MacBook Air M2";
    const countGoods = 3; // шт
    const categoryGoods = "Laptop";
    const categoryPrice = 112_000; // ₽

    console.log(nameGoods);
    console.log("Общая сумма товаров: " + (countGoods * categoryPrice));
}

{
    const nameGoods = "BMW S1000RR";
    const countGoods = 1; // шт
    const categoryGoods = "Moto";
    const categoryPrice = 980_000; // ₽

    console.log(nameGoods);
    console.log("Общая сумма товаров: " + (countGoods * categoryPrice));
}